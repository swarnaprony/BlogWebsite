<%- include("partials/header"); -%>

    <h1>
        Home
    </h1>

    <p> <%= homeStarting %> </p>


    <% posts.forEach(function(post) { %>
        <div class="title">
            <h2>
                <%= post.title %>
            </h2>
            <p>
                <%= post.content %>
            </p>
        </div>
        <% }) %>

<%- include("partials/footer"); -%>